Default admin, manager, pauser, migrator.
默认管理员、管理器、暂停器、迁移者。
[folks-finance-staking-contracts-reports](https://reports.immunefi.com/folks-finance-staking-contracts?utm_source=boost_program_page&_gl=1*x5xff4*_gcl_au*MTk5NTA5MTE2OS4xNzc2MjY4MzIz*_ga*MzY4NTY0NDkuMTc1NjczMzQwOQ..*_ga_JPHMK6RZT0*czE3NzY5MjM2NzckbzQkZzEkdDE3NzY5MjM4NjYkajYwJGwwJGg5ODE2MzE4ODE.)


要学习的：
[incorrect guard in setmigrationpermit prevents revocation after role removal breaking documented user control](https://reports.immunefi.com/folks-finance-staking-contracts/69376-sc-low-incorrect-guard-in-setmigrationpermit-prevents-revocation-after-role-removal-breaking-d)

[insufficient event emission in migratepositionsfrom leads to loss of migration accounting visibility](https://reports.immunefi.com/folks-finance-staking-contracts/68970-sc-insight-insufficient-event-emission-in-migratepositionsfrom-leads-to-loss-of-migration-acco)




一个全局的质押周期配置数组stakingPeriods：  通过 periodIndex来指向
一个用户的质押配置结构体map userStakes： 通过 address ->用户质押的次数-> userStake
用户怎么知道userStake对应的stakingPeriod？
不需要知道。因为在拿钱的时候，直接userStake拿钱退出。


有三个时间：
stakeTime：第10秒   
unlockTime:第100秒 质押stakingDurationSeconds为90秒
unlockDuration:50秒  说明第150秒才能全部提取

## scope
https://github.com/Folks-Fina...rc/interfaces/IMigratorV1.sol
	IMigratorV1.sol	11 March 2026
https://github.com/Folks-Fina...src/interfaces/IStakingV1.sol
	IStakingV1.sol	11 March 2026
https://github.com/Folks-Fin...ts/blob/main/src/Staking.sol
	Staking.sol	11 March 2026


Users deposit an ERC-20 token into the staking contract for a fixed duration and earn a pre-determined APR-based reward. Rewards are reserved at deposit time — the contract rejects new deposits if it cannot cover the promised payout from its current balance. When the staking period ends, both principal and reward unlock linearly over a separate unlock duration, allowing partial withdrawals at any point.
用户将ERC-20代币以固定期限存入质押合约，并获得预设的基于年利率的奖励。奖励在存款时预留——如果合同无法从当前余额支付承诺的支付，则拒绝新存款。质押期结束后，本金和奖励都会在不同的解锁期限内线性解锁，允许在任何时间点进行部分取款。

The contract also supports a migration mechanism that allows users to opt-in to having their open positions moved to a next-version staking contract, with all position data and tokens transferred atomically.
合约还支持迁移机制，允许用户选择将未开放仓位迁移到下一版本质押合约，所有头寸数据和代币都以原子方式传输。
Protection Mechanism 保护机制

## 不变量

The contract maintains two accumulators that track all outstanding obligations:
合同维护两个累加器，用于追踪所有未偿债务：

    activeTotalStaked — sum of all principal not yet withdrawn
    activeTotalStaked ——尚未提取的全部本金总和
    activeTotalRewards — sum of all rewards not yet withdrawn
    activeTotalRewards ——所有尚未提取的奖金总和

Every new deposit checks that the contract's current balance (before receiving the new deposit) is enough to cover all existing obligations plus the newly promised reward:
每笔新存款都会核对合同当前余额（在收到新存款前）是否足以覆盖所有现有义务及新承诺的奖励：
```shell
require(activeTotalStaked + activeTotalRewards + reward <= contractBalance);
```
Because the user's amount is transferred in during the same transaction, the contract balance grows by amount while the new total obligation grows by amount + reward. The check only needs to verify the protocol can cover reward — the principal is self-funded by the depositor.
因为用户的金额是在同一笔交易中转入的，所以合约余额会随着金额的增加而增加，而新的总债务会随着金额+奖励的增加而增长。支票只需要验证协议是否可以覆盖奖励——本金由存款人自筹资金。

This ensures the contract always holds sufficient tokens to pay every current and future obligation. The staking token must be a standard ERC-20: fee-on-transfer and rebasing tokens would break this invariant.
这确保合约始终持有足够的代币来支付所有当前和未来的债务。质押代币必须是标准的ERC-20：转账手续费和重置代币会破坏这一不变性。

## These aren’t necessarily “issues”, some are design decisions and tradeoffs:
这些不一定是“问题”，有些是设计决策和权衡：

    Not checking zero address
    没有检查零地址
    StakeParams slippage is one directional by design
    StakeParam 滑点设计为单向滑点
    User will have to use new account if they reach staking limit
    如果用户达到质押上限，就必须使用新账户
    Not deleting “UserStake” state after everything has been withdrawn
    在所有内容撤回后，没有删除“UserStake”状态
    Not deleting “StakingPeriod” state after deactivating
    停用后不删除“质押期间”状态
    Updates to a “StakingPeriod” only impact new stakes by design
    “质押期间”的更新设计上只会影响新的赌注
    We allow stake with 0 rewards
    我们允许0奖励的质押
    We allow withdrawal of 0 amount
    我们允许0元的取款
    We intentionally don’t allow a partial stake amount if the entire amount would cause cap to be exceed
    如果部分本金会导致上限超过，我们故意不允许部分本金
    Operational risk of migration
    迁移的操作风险
    MIGRATOR_ROLE persists for user after migration
    迁移后用户MIGRATOR_ROLE依然存在
    State “migrationPermits” may contain migrator which had its MIGRATOR_ROLE later revoked
    州“移民许可证”可能包含后来被撤销MIGRATOR_ROLE的迁徙者
    After migration, the indexes of the “userStakes” are shuffled. This could lead to a user referencing an outdated index.
    迁移后，“用户权益”的索引会被洗牌。这可能导致用户引用过时的索引。
    Paused contract only prevents new stakes
    暂停合同只会阻止新的赌注
    “UserStake.aprBps” is for informational purposes
    “UserStake.aprBps” 仅供参考
    The function “stakeWithPermit” silently ignores permit failure
    函数“stakeWithPermit”默默忽略许可失败
    Reward and accrual calculations round down
    奖励和累计计算向下取整
    Intentional to not decrease “capUsed” on withdrawal / migration
    有意避免在取出/迁移时减少“capUsed”
    Staking contract designed for ERC20 which doesn't have any fee on transfer or rebasing logic
    为ERC20设计的质押合约，对转移或重新基于逻辑没有任何费用

